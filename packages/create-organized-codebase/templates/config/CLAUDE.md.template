# {{projectName}}

## Tech Stack
- Runtime: {{techStack}}
- Framework: [Add your framework]
- Database: [Add if applicable]
- Key Dependencies: [List major packages]

## Project Structure
```
src/           → Source code
tests/         → Test files
docs/          → Documentation
PLANNING/      → Project planning docs
.claude/       → Claude Code configuration
```

## Code Conventions
- Use TypeScript with strict mode
- Prefer async/await over callbacks
- Use conventional commits (feat/fix/docs/refactor/test/chore)
- Keep functions under 50 lines

## DO NOT
- Never use `any` type in TypeScript
- Never commit with failing tests
- Never use `--dangerously-skip-permissions`
- Never force push to main/master
- Never commit secrets or credentials

## Verification Requirements
Before completing ANY task:
1. Describe your verification approach
2. Run `npm run lint` (no errors)
3. Run `npm run typecheck` (no errors)
4. Run `npm test` (all passing)
5. For UI changes: provide screenshot or description

---
*Generated by create-organized-codebase v{{version}} on {{timestamp}}*
